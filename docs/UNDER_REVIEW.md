# Biological Foundation Models — records under review

Audited candidates excluded from the displayed canonical index until their scope, artifact, identity, duplication, or reuse-term issue is resolved.

These candidates are **not part of the displayed or canonical released index**. Their stable IDs are reserved so that decisions remain traceable. This register is a review queue, not a recommendation or tombstone list.

Register date: 2026-07-14 · Records under review: 32

| Stable ID | Candidate | Reason class | Why held | Reconsider when |
| --- | --- | --- | --- | --- |
| `bfm-mrna-fm-plantrna-fm` | mRNA-FM / PlantRNA-FM | family_identity | only PlantRNA-FM has a primary paper link and the two model cards carry different licences. Split or fully evidence both components. | Split the family or verify each member with its own primary evidence, artifact locator, and licence. |
| `bfm-saturn` | SATURN | artifact | repository provides training code, embeddings and derived macrogene weights, not a reusable pretrained SATURN checkpoint. HOLD or reclassify as method. | Reconsider when an official reusable pretrained SATURN checkpoint is released and its terms are verified, or reclassify outside model scope. |
| `bfm-xtrimogene` | xTrimoGene | artifact | paper-only with no official code or checkpoint. HOLD under the reusable-artifact rule. | Reconsider when official reusable code or a checkpoint is released and verified. |
| `bfm-scprotein` | scPROTEIN | scope | dataset-specific analysis framework/checkpoints do not substantiate a broadly pretrained foundation model. HOLD or reclassify outside scope. | Reconsider if broader reusable pretraining is evidenced or a written scope-policy revision explicitly includes this class. |
| `bfm-deepsea` | DeepSEA | scope | Identity is correct, but the linked artifact is the later Selene framework, and DeepSEA is a supervised regulatory predictor rather than an unambiguous reusable FM. Decide whether it is a documented historical/canonical exception. Paper, Selene. | Reconsider only under a written historical-exception policy with the original artifact correctly identified. |
| `bfm-chrombpnet` | ChromBPNet | scope | Current paper/repo describe a bias-factorized chromatin-accessibility predictor trained per assay, not clearly a reusable multi-task foundation model. Reassess against the stated scope rule or document an exception. Paper, repo. | Reconsider if a written scope policy includes assay-specific predictors or broader reusable pretraining is evidenced. |
| `bfm-mistral-dna` | Mistral-DNA | multiple | no primary paper, linked checkpoint currently unauthorized, and direct model-card URL is absent. HOLD until checkpoint and terms are verifiable. | Reconsider when a primary source, authorized checkpoint, direct model card, and exact terms are all verified. |
| `bfm-florabert` | FloraBERT | scope | Official description focuses on plant promoter modelling and tissue-specific gene-expression prediction; broad reusable FM scope is not established. Paper, repo. | Reconsider if broader reusable foundation-model scope is established by primary evidence. |
| `bfm-drfold` | DRfold | scope | DRfold/DRfold2 are RNA structure predictors; DRfold2 contains a pretrained RCLM component, but the catalogue record itself remains one-task structure prediction. HOLD or define a structural-model exception. DRfold paper, DRfold2 paper, DRfold, DRfold2. | Reconsider only under a written structural-model exception or after reframing around an evidenced reusable pretrained component. |
| `bfm-framediff-frameflow-multiflow` | FrameDiff / FrameFlow / Multiflow | family_identity | Improperly merges three distinct methods while citing only Multiflow’s paper/repo. Either keep Multiflow alone or add separate FrameDiff and FrameFlow evidence/records. Multiflow ICML, Multiflow repo, FrameDiff paper, FrameDiff repo, FrameFlow paper, FrameFlow repo. | Split FrameDiff, FrameFlow, and MultiFlow into independently evidenced records, or narrow this record to one system. |
| `bfm-protriever` | Protriever | artifact | official repository contains only README, not open code or weights. HOLD and correct status to placeholder/paper only. | Reconsider when official executable code or reusable weights are released and verified. |
| `bfm-protgps` | ProtGPS | scope | Model is centred on one localization objective—12-compartment prediction and localization-conditioned generation. Reassess broad-FM eligibility. Paper, repo. | Reconsider if broader reusable foundation-model scope is established or a written task-specific exception is adopted. |
| `bfm-neuralplexer-and-neuralplexer3` | NeuralPLexer (and NeuralPLexer3) | family_identity | Family conflates v1 and NP3. V1 checkpoints are explicitly CC BY-NC-SA for non-commercial use, while linked repo/paper do not implement NP3. NP3 has a separate NeurIPS paper and no verified public code/weights in the cited artifact. v1 paper, v1 repo, v1 checkpoints, NP3 paper. | Split NeuralPLexer v1 and NP3 and verify each version's implementation, weights, and terms. |
| `bfm-uni-mol-docking-v2` | Uni-Mol Docking V2 | scope | Exact model is a dedicated docking predictor rather than an unambiguous broader-than-one-task FM. HOLD or document a structural/docking exception. Paper, repo. | Reconsider only under a written structural/docking exception or with evidence of broader reusable scope. |
| `bfm-abodybuilder3` | ABodyBuilder3 | scope | Identity and artifacts verify, but ABodyBuilder3 is a task-specific antibody structure predictor rather than a model broader than one task; HOLD or document a structural-model exception. Paper, repo. | Reconsider only under a written structural-model exception or with evidence of broader reusable scope. |
| `bfm-scdiva` | ScDiVa | artifact | paper-only with no released code or checkpoint. HOLD under the reusable-artifact rule. | Reconsider when official reusable code or a checkpoint is released and verified. |
| `bfm-tabula` | Tabula | artifact | set NC true, but repository has training code only and no pretrained checkpoint. HOLD until reusable weights exist. | Reconsider when an official reusable pretrained checkpoint is released and its terms are verified. |
| `bfm-virchow-virchow2-and-virchow2g` | Virchow / Virchow2 (and Virchow2G) | family_identity | Base paper; base model; official family access. Action: split the family or encode member-specific terms: base Virchow is gated Apache-2.0, Virchow2 is non-commercial research, and Virchow2G has different commercial access. | Split the family or encode member-specific artifacts, access routes, and terms with primary evidence. |
| `bfm-h-optimus-0-h-optimus-1` | H-optimus-0 / H-optimus-1 | family_identity | “Paper” is a company announcement, H-optimus-1 is missing from artifacts and is gated CC BY-NC-ND. Add both model cards and accurate mixed terms. | Verify and link each model card and encode the family members' distinct access and reuse terms. |
| `bfm-atlas-rudolfv-successor` | Atlas (RudolfV successor) | artifact | Listed HF artifact returns no model; official Aignostics materials now describe flexible commercial licensing and Atlas 2, not open/gated Atlas weights. Replace the dead artifact and avoid “open code, gated weights” unless a current public release is found. Atlas paper, dead listed artifact, official model page, Atlas 2. | Reconsider when a current official public artifact or endpoint and its exact access terms are verified. |
| `bfm-bioreason` | BioReason | artifact | repository still says checkpoints are forthcoming. HOLD until a reusable checkpoint is released. | Reconsider when the promised official checkpoints are released and their terms are verified. |
| `bfm-openprotein-ai-poet-poet-2` | OpenProtein.AI (PoET / PoET-2) | duplication | set NC true and resolve duplication with bfm-poet-2. Narrow to the commercial platform or merge. | Narrow to the platform or merge with the PoET-2 record while documenting the stable-ID disposition. |
| `bfm-pearl` | Pearl | artifact | Only a paper is cited; “Web/API/commercial” lacks an official artifact locator. Official Genesis pages describe Pearl and an AI platform/partner access, not a public model endpoint. Paper, official Pearl page. | Reconsider when an official reusable model artifact or public endpoint and its terms are verified. |
| `bfm-sctranslator` | scTranslator | multiple | Code/checkpoints are public but no licence file is present, and the model is a single RNA-to-protein translation task. HOLD or explicitly document both scope and no-licence limitations. Paper, repo. | Reconsider when scope eligibility and explicit code/checkpoint reuse terms are both resolved. |
| `bfm-chemberta-3` | ChemBERTa-3 | multiple | Paper; artifact. Action: reclassify as a training platform/framework and change “open” to “public source/model links; reuse licence unconfirmed/HOLD”. | Reconsider after platform scope and explicit reuse terms for the released framework/model artifacts are verified. |
| `bfm-genecompass` | GeneCompass | terms | Paper; artifact. Action: replace “open” with “public code and checkpoints; reuse licence unconfirmed/HOLD”. | Reconsider when explicit code and checkpoint reuse terms are confirmed from an official source. |
| `bfm-hybridna` | HybriDNA | artifact | Paper; no provenance-confirmed official artifact found. Action: retain paper-only/HOLD. | Reconsider when a provenance-confirmed official reusable model artifact is released and verified. |
| `bfm-genemamba` | GeneMamba | terms | Paper; code; weights. Action: qualify as public code/weights with no confirmed reuse licence; HOLD the “open” claim. | Reconsider when explicit reuse terms for the official code and weights are confirmed. |
| `bfm-scmmgpt` | scMMGPT | multiple | Paper; repository. Action: change “open code” to “public source skeleton; weights and licence unconfirmed/HOLD”. | Reconsider when official reusable weights and explicit reuse terms are both confirmed. |
| `bfm-plip-pathology-language-image-pretraining` | PLIP (Pathology Language-Image Pretraining) | terms | Paper; code; weights. Action: change “open” to “public code/weights; reuse terms unresolved/HOLD”; neither current official location provides a confirmed licence. | Reconsider when explicit reuse terms for the official code and weights are confirmed. |
| `bfm-next-mol` | NExT-Mol | terms | Paper; artifact; weights. Action: qualify as public code/checkpoints with reuse licence unconfirmed/HOLD. | Reconsider when explicit reuse terms for the official code and checkpoints are confirmed. |
| `bfm-lingshu-cell` | Lingshu-Cell | artifact | Paper; official collection; project. Action: downgrade “model hub” to paper/project page; the official collection currently contains the paper, not a model. | Reconsider when the official collection contains a released reusable model artifact with verified terms. |

## Promotion rule

A record may move into models_final.json only after the stated reconsideration condition is met, primary evidence is refreshed, and the full maintenance gate passes.

The machine-readable source is `records_under_review.json`; its contract is `records_under_review.schema.json`. The dated source-review ledgers remain the evidence authority.
